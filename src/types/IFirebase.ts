export interface IAbout {
    about: string;
}

export interface IAvatar {
    imageUrl: string;
    socialUrl: string;
}

export interface IBlock {
    title: string;
    date: string;
    message: string;
}

export interface IName {
    name: string;
}
